身份证号码

220523200907054437


测试查训

select od.order_id, *  
  from  ticketassign ta  
  inner join orderdetail od on ta.orderdetail_id=od.id
  inner join ticketprice tp on tp.id=od.ticketprice_id
inner join ticket tk on tp.ticket_id=tk.id
inner join scenic si on tk.scenic_id=si.id and si.id=10